import { getFirebaseAdminDb } from "./lib/firebase/admin"; import { generateBrief } from "./lib/ai/brief"; import * as dotenv from "dotenv"; dotenv.config({ path: ".env.local" }); async function run() { try { const db = getFirebaseAdminDb(); console.log("Firebase initialized"); const snap = await db.collection("cases").limit(1).get(); console.log("Cases fetched:", snap.size); const result = await generateBrief([{ id: "test", case_number: "test", status: "received", created_at: new Date() } as any]); console.log("Brief:", result.brief); } catch(e) { console.error("ERROR:", e); } } run();
